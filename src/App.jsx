import "./App.css"
import Die from "./components/Die"

function App() {
	return (
		<main>
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
			<Die value={1} />
		</main>
	)
}

export default App
